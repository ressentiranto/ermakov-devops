name: Basic CI

on:
  push:
    branches: [ master ]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout репозитория
        uses: actions/checkout@v4

      - name: Просто вывести сообщение
        run: echo "GitHub Actions работают! 🎉"
